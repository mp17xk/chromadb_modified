from setuptools import setup, find_packages

setup(
    name="chromadb",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # Elenca qui le eventuali dipendenze di cui ha bisogno il tuo pacchetto
    ],
    author="IlTuoNome",
    description="Versione modificata di chromadb",
    url="https://github.com/mp17xk/chromadb_modified",
)
